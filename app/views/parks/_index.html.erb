<%= render 'shared/navbar-top' %>
<h1>Dog Playgrounds of Berlin</h1>

<div id="map"
     style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>

<h2><%= park.name %></h2>
<p><%= park.address %></p>
